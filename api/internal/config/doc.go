// Package config loads and validates the ranking configuration
// (config/ranking.yaml).
//
// The struct it returns is consumed by the rank package; this package owns
// neither defaults nor scoring logic, only parsing.
package config
